import { NivelColorPipe } from './nivel-color-pipe';

describe('NivelColorPipe', () => {
  it('create an instance', () => {
    const pipe = new NivelColorPipe();
    expect(pipe).toBeTruthy();
  });
});
